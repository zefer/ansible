# usage: ansible-playbook -i inventory -K music_server.yml --ask-vault-pass

- hosts: music_servers
  sudo: true
  vars:
  tasks:
  roles:
    - music_server
    - mpdscribble
    - mothership
    - mpdlcd
